---
spring:
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        serviceId: SAMPLE-HYSTRIX-CONFIG

eureka:
  instance:
    nonSecurePort: ${server.port:8089}
  client:
    serviceUrl:
      defaultZone: http://http://http://18.212.48.4:8761/eureka
